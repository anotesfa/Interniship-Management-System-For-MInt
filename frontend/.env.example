# API Configuration
# Use /api/v1 with Vite proxy (dev) or full URL when not using the dev server
VITE_API_BASE_URL=/api/v1

# Application Configuration
VITE_APP_NAME=MInT Internship Management System
VITE_APP_VERSION=1.0.0
